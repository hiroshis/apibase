spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/springboot-flyway
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver
  flyway:
    baseline-on-migrate: true
    locations: classpath:/db/migration